name: Hello Workflow

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "🎉 Hello Sonalika! Your first GitHub Action is running successfully."
